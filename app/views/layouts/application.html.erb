<!DOCTYPE html>
<html>
  <head>
    <title>GiftWise - Smart Gift Planning</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <% unless Rails.env.test? %>
      <%= javascript_importmap_tags %>
    <% end %>
  </head>

  <body class="d-flex flex-column" style="min-height: 100vh;">
    <!-- Navigation Header -->
    <header class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="<%= root_path %>">
          <i class="bi bi-gift"></i> GiftWise
        </a>
        
        <% if logged_in? %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarNav">
            <nav class="navbar-nav ms-auto gap-2 align-items-lg-center">
              <%= button_to "Home", root_path, method: :get, class: "btn btn-outline-light btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
              <%= button_to "Contacts", contacts_path, method: :get, class: "btn btn-outline-light btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
              <%= button_to "Events", events_path, method: :get, class: "btn btn-outline-light btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
              <%= button_to "Wish List", wish_list_items_path, method: :get, class: "btn btn-outline-light btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
              <%= button_to "Settings", settings_path, method: :get, class: "btn btn-outline-light btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
              <%= button_to "Logout", session_path, method: :delete, class: "btn btn-outline-danger btn-sm", style: "padding: 0.375rem 0.75rem; margin-top: 0;" %>
            </nav>
          </div>
        <% end %>
      </div>
    </header>

    <!-- Flash Messages -->
    <div class="container-fluid">
      <% flash.each do |k, v| %>
        <div class="alert alert-<%= k == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= v %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <!-- Main Content -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-4 mt-auto">
      <div class="container-fluid">
        <p class="mb-0">&copy; <%= Time.current.year %> GiftWise. Plan smarter, gift better.</p>
      </div>
    </footer>

    <!-- Bootstrap Bundle JS (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
