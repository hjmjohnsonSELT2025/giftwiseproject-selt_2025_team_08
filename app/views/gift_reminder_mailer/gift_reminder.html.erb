<h1>Gift Reminder for <%= h(@event.name) %></h1>

<p>Hi <%= h(@user.first_name) %>,</p>

<p>Your event <strong><%= h(@event.name) %></strong> is coming up! Here's a summary of your gift status:</p>

<h2>Gift Status Summary</h2>

<% if @gift_summary&.any? %>
  <% @gift_summary.each do |recipient, gifts| %>
    <div style="margin-bottom: 20px; padding: 15px; background-color: #f5f5f5; border-left: 4px solid #4CAF50;">
      <h3><%= h(recipient.first_name) %> <%= h(recipient.last_name) %></h3>
      
      <% if gifts[:selected]&.any? %>
        <p><strong style="color: #4CAF50;">✓ Gift Selected:</strong></p>
        <ul>
          <% gifts[:selected].each do |gift| %>
            <li><%= h(gift.idea) %> (<%= number_to_currency(gift.price) if gift.price %>)</li>
          <% end %>
        </ul>
      <% else %>
        <p><strong style="color: #f44336;">✗ No Gift Selected Yet</strong></p>
        
        <% if gifts[:suggestions]&.any? %>
          <p><strong>Suggested Gift Ideas:</strong></p>
          <ul>
            <% gifts[:suggestions].each do |suggestion| %>
              <li><%= h(suggestion) %></li>
            <% end %>
          </ul>
        <% end %>
      <% end %>
    </div>
  <% end %>
<% else %>
  <p>No gift summary available at this time.</p>
<% end %>

<p>Time to get shopping!</p>

<p>Best regards,<br>
The Gift Wise Team</p>
