<% if @contact.save %>
  document.getElementById('note-<%= @contact.id %>').textContent = '<%= @contact.note %>';
  
  const modal = bootstrap.Modal.getInstance(document.getElementById('noteModal-<%= @contact.id %>'));
  if (modal) {
    modal.hide();
  }
<% else %>
<% end %>
